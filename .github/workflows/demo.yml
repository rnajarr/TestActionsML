name: GitHub Actions Demo with Docker
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: push
jobs:
  Explore-GitHub-Actions:
    name: Run Matlab in docker
    runs-on: ubuntu-latest
    container:
      image: mathworks/matlab:latest
      volumes:
        - my-app/app
    steps:
      - name: Running matlab
        run: docker run -it -e MLM_LICENSE_FILE=7350@linuxcls10.dearborn.ford.com --mount type=bind,source="C:/Users/RNAJAR/Desktop",target=/scripts mathworks/matlab:r2021a matlab -nodisplay -nosplash -nodesktop -r "run('/scripts/example.m');exit;"
